#include <iostream>
using namespace std;

main()
{
    int x[5] = {1, 2, 3, 4, 5};
    int found = 5, search_value;
    cout << "Enter the value";
    cin >> search_value;
    for (int i = 0; int <= 4; i++)
    {
        if (search_value == x[i])
        {
            cout << "Value is found...";
            found++;
            break;
        }
        if (found == 0)
        {
            cout << "n\n Value can't be found";
        }
        return x[current]
    }

    return 0;
}